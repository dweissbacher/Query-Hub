# --- Query Metadata ---
# Human-readable name for the query. Will be displayed as the title.
name: Suspicious Network Connections

# Unique identifier for the query.
id: T1071.001-web-protocols-c2

# Description of what the query does and its purpose.
description: This query identifies suspicious network connections that may indicate command and control communication, including connections to uncommon ports and suspicious user agents.

# The author or team that created the query.
author: Your Security Team

# The required log sources to run this query successfully in Next-Gen SIEM.
# This will be displayed in the UI to inform the user.
log_sources:
  - Endpoint
  - NetworkConnectIP4
  - ProcessRollup2

# Tags for filtering and categorization.
# Include relevant techniques, tactics, or platforms.
tags:
  - Command and Control
  - Network
  - C2 Communication
  - MITRE T1071.001

# --- Query Content ---
# The actual CrowdStrike Query Language (CQL) code.
# Using the YAML block scalar `|` allows for multi-line strings.
cql: |
  #event_simpleName=NetworkConnectIP4 
  | (RemotePort in [4444, 8080, 9999, 6666, 1337] OR LocalPort in [4444, 8080, 9999, 6666, 1337])
  | !RemoteAddressIP4=/(^10\.|^172\.(1[6-9]|2[0-9]|3[01])\.|^192\.168\.|^127\.)/
  | join({#event_simpleName=ProcessRollup2}, field=[aid, ContextProcessId], include=[ImageFileName, CommandLine])
  | join({#event_simpleName=UserIdentity}, field=AuthenticationID, include=[UserName])
  | table([aid, UserName, ImageFileName, RemoteAddressIP4, RemotePort, LocalPort, CommandLine])